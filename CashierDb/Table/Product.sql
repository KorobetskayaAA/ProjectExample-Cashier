CREATE TABLE [dbo].[Product]
(
	[Barcode] BIGINT NOT NULL PRIMARY KEY CHECK([Barcode] BETWEEN 1000000000000 AND 9999999999999)
   ,[Name] NVARCHAR(100) NOT NULL CHECK(LEN([Name]) > 0)
   ,[Price] DECIMAL(15,2) NOT NULL CHECK([Price] > 0)
)
